<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DayMirror.FinishedActionDetails">
    <ContentPage.Content>
        <StackLayout>
            <Label 
                Text="{Binding Title}"/>
            <Label 
                Text="{Binding StartTime, StringFormat='Start time {0:hh\\:mm\\:ss}'}"
                />
            <Label 
                Text="{Binding EndTime, StringFormat='Start time {0:hh\\:mm\\:ss}'}"/>
            <Label 
                x:Name="TimeElapsedLabel"/>
            <Button 
                Text="New action"
                Clicked="OnCreateActionButtonClicked"/>
            <Button
                Text="Main menu"
                Clicked="OnGoToMainMenuButtonClicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>